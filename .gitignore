# OS / editor
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# Env / secrets
.env
.env.*
!.env.example

# Build output
dist/
build/
.cache/

# suburb/ is generated by tools/build-suburbs.py but committed to the repo
# so Railway can serve it without needing Python at deploy time.
# Re-run `npm run build` after editing data/gold-coast-opshops.json.

# Local serve / temp
*.log
/tmp/
.serve/
